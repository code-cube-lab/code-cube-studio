# Static v1 does not require environment variables.
# Add public analytics IDs or server integration variables only after choosing hosting.
# Never commit bot tokens, API keys, payment credentials, or private identifiers.
